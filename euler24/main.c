#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, i, count=0, k, sum=0,j, x;
    int numbers[]={0,1,2,3};
    int permutations[9999];
    for(i=0;i<4;i++){
        permutations=numbers[i]*1000+numbers[i+1]*1000+numbers[i]*1000+numbers[i]*1000
    }
    //while(1){
       // printf("yarak ahmet");

   // }
    }

